What sort of error message will you see, if ...

1. You remove a semi-colon from a PHP file

2. You leave out the `<script>` tag that loads Vue.js

3. You remove the `id` attribute from the HTML
   element that contains your VueJS application.


Given these symptoms, what is the root cause?

4. DbConnect class won't load

5. PHP files won't load in Atom's HTML preview

6. `MYSQL_HOST` is not defined
